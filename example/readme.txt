Please generate private_key.pem
ssh-keygen -m PEM -t rsa -f private_key.pem

and install

pip install PyserSSH[ext_pyofetch]
pip install PyserSSH[RemoDesk]
pip install beautifulsoup4 natsort asciichartpy markdownify
